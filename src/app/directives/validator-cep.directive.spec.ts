import { ValidatorCepDirective } from './validator-cep.directive';

describe('ValidatorCepDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorCepDirective();
    expect(directive).toBeTruthy();
  });
});
